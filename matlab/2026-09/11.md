# 2026-09-11 · 星期五

## 今天做了什么

- 复习巩固状态空间（前天"刚开了个头"，今天彻底吃透）
- 从"忘完了"重新推导出完整状态空间：
  - `x = [角度; 角速度]`（照片：状态）
  - `x' = [角速度; 角加速度]`（录像：变化率）
  - 澄清"角速度"的双重身份（既是状态，又是角度的变化率）
- 亲手推出 `x' = [x₂; -(g/L)·x₁]`，填出 `A = [0 1; -(g/L) 0]`
- 在 MATLAB 验证 `A*x` 与匿名函数 `@(t,z)[z(2); -(g/L)*z(1)]` 结果相同

## 踩坑 / 收获

- **矩阵乘法 = 配方表**：A 每一行是一个"配方"，调出一杯"变化率"
- **-(g/L) = ω₀²**：量纲分析发现它是固有角频率的平方，串联周期 T = 2π√(L/g) ≈ 2.007s
- **角频率 ω₀ ≠ 角速度 θ'**：前者是常数（固有频率），后者是状态变量（随时变）
- **A 矩阵位置含义**：行 = 谁的变化率，列 = 由哪个状态决定
- 矩阵格子里只放系数，不放变量（x 已经在右边了）

## 明天计划

- 台阶3收尾：用矩阵 A 直接求解（expm），对比 ode45，理解"状态转移矩阵"

## 代码文件

- 今天主要是复习推导，无新代码文件
